{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：core + store + 麦克风（用于 STT 与录音）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
